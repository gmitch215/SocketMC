{
  "schemaVersion": 1,
  "id": "${project.ext.id}",
  "version": "${project.version}",
  "entrypoints": {
    "client": [
      "me.gamercoder215.socketmc.fabric.FabricSocketMC"
    ],
    "modmenu": [
      "me.gamercoder215.socketmc.fabric.modmenu.SocketModMenu"
    ]
  },

  "name": "${project.ext.name}",
  "description": "${project.description}",
  "authors": ["${project.ext.author}"],
  "contact": {
    "homepage": "${project.ext.github}",
    "sources": "${project.ext.github}/tree/master",
    "issues": "${project.ext.github}/issues"
  },
  "mixins": [
    "socketmc-fabric.mixins.json"
  ],
  "accessWidener": "socketmc.accesswidener",

  "license": "${project.ext.license}",
  "icon": "assets/icon.png"
}